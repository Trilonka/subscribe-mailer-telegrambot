{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "echo start_build"
  },
  "deploy": {
    "startCommand": "start.sh $BOT_USERNAME $BOT_TOKEN",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "never"
  }
}
